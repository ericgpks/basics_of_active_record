<!DOCTYPE html>
<html>
  <head>
    <title>Rails7022Ruby311ArApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header>
      <nav>
        <ul style="list-style-type: none; padding: 10px; background-color: #f8f9fa; margin: 0; display: flex; gap: 20px;">
          <li><%= link_to "Home", root_path rescue link_to "Home", "/" %></li>
          <li><%= link_to "Users", users_path %></li>
          <li><%= link_to "Books", books_path %></li>
          <li><%= link_to "Sign Up", signup_path %></li>
        </ul>
      </nav>
    </header>
    
    <% if notice %>
      <div style="padding: 10px; background-color: #d4edda; color: #155724; margin: 10px 0;">
        <%= notice %>
      </div>
    <% end %>
    
    <% if alert %>
      <div style="padding: 10px; background-color: #f8d7da; color: #721c24; margin: 10px 0;">
        <%= alert %>
      </div>
    <% end %>
    
    <main style="padding: 20px;">
      <%= yield %>
    </main>
  </body>
</html>
